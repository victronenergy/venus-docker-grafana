version: '3.4'
services:
  server:
    image: "sbender9/victron-server:amd64-latest"
    network_mode: host
    ports:
     - "8088:8088"
    volumes:
     - "config-storage:/config"
  influxdb:
    image: "influxdb:1.7"
    ports:
     - "8086:8086"
    volumes:
     - "influxdb-storage:/var/lib/influxdb"
    environment:
     - INFLUXDB_HTTP_LOG_ENABLED=false
  grafana:
    image: "sbender9/victron-grafana:amd64-latest"
    volumes:
     - "grafana-storage:/var/lib/grafana"
    ports:
     - "3000:3000"
    
volumes:
 influxdb-storage:
 grafana-storage:
 config-storage:

